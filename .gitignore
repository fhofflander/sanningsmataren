# Dependencies
node_modules

# Build output
dist
dist-ssr
dist-extension
*.tsbuildinfo

# Local env files - NEVER commit real API keys.
# Vite loads .env*.local; *.development.local is dev-only and is never
# inlined into a production build.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.local
