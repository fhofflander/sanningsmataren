// Types now live in debatt/format, shared with validation and compilation.
// The viewer renders timeline.json version 2; v1 files are upgraded in
// memory by upgradeTimeline (see App.tsx).

export * from "../../format/src/types";
export * from "../../format/src/indexDoc";
